import { FechaConHoraPipe } from './fecha-con-hora.pipe';

describe('FechaConHoraPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaConHoraPipe();
    expect(pipe).toBeTruthy();
  });
});
